This directory contains two very simple Python Command Line Tools.

To bundle both tools into a Docker image, run:

```
docker build -t ecerami/extract .
```

I have pushed the image to:  https://hub.docker.com/repository/docker/ecerami/extract